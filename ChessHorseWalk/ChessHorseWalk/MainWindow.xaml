<Window x:Class="ChessHorseWalk.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChessHorseWalk"
        mc:Ignorable="d"
        Title="HorseWalk" Height="650" Width="600">
    <DockPanel>
        <Grid DockPanel.Dock="Bottom">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Button
                x:Name="_butReset"
                Content="Reset" />
            <Button
                Grid.Column="1"
                x:Name="_butStart"
                Content="Start" />
            <Button
                Grid.Column="2"
                x:Name="_butCancel"
                Content="Cancel"
                IsCancel="True" />
        </Grid>
        <local:ChessField x:Name="_field"/>
    </DockPanel>
</Window>
